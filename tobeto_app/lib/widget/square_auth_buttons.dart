
import 'package:flutter/material.dart';

class SquareAuthButtons extends StatelessWidget {
const SquareAuthButtons({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}