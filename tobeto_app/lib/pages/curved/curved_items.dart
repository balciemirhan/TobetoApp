import 'package:flutter/material.dart';

List<Icon> curvedItems() {
  final items = [
    const Icon(
      Icons.home,
      /* color: Colors.white, */
    ),
    const Icon(
      Icons.bookmark,

      /* color: Colors.white, */
    ),
    const Icon(
      Icons.account_circle,
      /* color: Colors.white, */
    ),
  ];
  return items;
}
