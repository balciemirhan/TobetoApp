import 'package:flutter/material.dart';

List<Icon> curvedItems() {
  final items = [
    const Icon(
      Icons.home,
    ),
    const Icon(
      Icons.reviews_rounded,
    ),
    const Icon(
      Icons.account_circle,
    ),
  ];
  return items;
}
